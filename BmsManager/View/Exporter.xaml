<Window x:Class="BmsManager.Exporter"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BmsManager"
        mc:Ignorable="d"
        Title="Exporter" Height="450" Width="800">
    <Window.DataContext>
        <local:ExporterViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>

        </Grid.RowDefinitions>
        <TextBlock Grid.Column="0" Grid.Row="0" Text="songdata(beatoraja)" HorizontalAlignment="Right"/>
        <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding SongdataDBPath}"/>
        <TextBlock Grid.Column="0" Grid.Row="1" Text="songinfo(beatoraja)" HorizontalAlignment="Right"/>
        <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding SongInfoDBPath}"/>
        <Button Grid.Column="2" Grid.Row="2" Content="Export(beatoraja)" Command="{Binding Export}"/>
    </Grid>
</Window>
